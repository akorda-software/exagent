import Config

# The consumer owns SDK configuration; no default exporter or service is needed.
if System.get_env("PACKAGE_ACCEPTANCE_MODE") in ["sdk", "exporter"] do
  config :opentelemetry, processors: []
end
